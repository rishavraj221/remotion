# Ignore all AI-generated project files
generated-*.tsx
composition-*.tsx

# Ignore metadata and registry files
compositions-metadata.json
../ai-compositions-registry.tsx

# Note: current-project.tsx is tracked in git with default null export
# to prevent build errors. The backend overwrites it at runtime.

